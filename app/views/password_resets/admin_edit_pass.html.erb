<div>
	<%= simple_form_for(User.new, url: admin_change_pass_password_resets_path, html: { id: "change-password-form" }) do |f| %>
    <%= f.input :email, placeholder: 'email', required: true %>
    <%= f.button :submit, 'Изменить', class: 'btn btn-our btn-our-green fr' %>
  <% end %>
</div>